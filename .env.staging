# Database Configuration for Staging
DATABASE_URL=postgresql://yanuar:password@localhost:5432/invenflow_staging

# Backend Configuration
PORT=3001
NODE_ENV=production

# JWT Configuration for Staging
JWT_SECRET=staging-invenflow-secret-key-change-in-production-please
JWT_EXPIRES_IN=24h

# Stored Cleanup Configuration
STORED_SWEEP_INTERVAL_MINUTES=1

# Frontend Configuration
VITE_API_URL=https://staging.ptunicorn.id/api

# CORS Configuration for Staging
# Comma-separated list of allowed origins for CORS
CORS_ORIGIN=https://staging.ptunicorn.id,http://localhost:3001,http://localhost:5173
STAGING_DOMAIN=https://staging.ptunicorn.id
FRONTEND_URL=https://staging.ptunicorn.id

# Upload Configuration
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760

# Logging Configuration
LOG_LEVEL=info

# Security Configuration sdfsdf
BCRYPT_ROUNDS=12

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100