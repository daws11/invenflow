# Database Configuration
DATABASE_URL=postgresql://invenflow_prod:SecureProdPass123!@localhost:5432/invenflow_production

# Backend Configuration
PORT=3002
NODE_ENV=production

# JWT Configuration
JWT_SECRET=production-jwt-secret-key-change-this-in-production-environment
JWT_EXPIRES_IN=24h

# Frontend Configuration
VITE_API_URL=https://inventory.ptunicorn.id

# Security Configuration
BCRYPT_ROUNDS=12

# File Upload Configuration
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760

# Performance Configuration
UV_THREADPOOL_SIZE=128

# Stored Cleanup Configuration
STORED_SWEEP_INTERVAL_MINUTES=1

# Cache Configuration (BACKEND MEMBACA BAGIAN INI)
CACHE_ENABLED=false
CACHE_DEFAULT_TTL_MS=300000
CACHE_TAG_TTL_MS=3600000
CACHE_MAX_KEYS=20000
CACHE_WARM_INTERVAL_MS=300000

# Redis Configuration
# Either provide REDIS_URL or individual host/port/password values
REDIS_URL=
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_USERNAME=
REDIS_PASSWORD=
REDIS_TLS=false
REDIS_POOL_MIN=2
REDIS_POOL_MAX=10